{% extends 'base.html.twig' %}

{% block title %}SupportVisuel index{% endblock %}

{% block body %}
    <h1>SupportVisuel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Description</th>
                <th>Lien</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for support_visuel in support_visuels %}
            <tr>
                <td>{{ support_visuel.id }}</td>
                <td>{{ support_visuel.libelle }}</td>
                <td>{{ support_visuel.description }}</td>
                <td>{{ support_visuel.lien }}</td>
                <td>
                    <a href="{{ path('support_visuel_show', {'id': support_visuel.id}) }}">show</a>
                    <a href="{{ path('support_visuel_edit', {'id': support_visuel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('support_visuel_new') }}">Create new</a>
{% endblock %}
