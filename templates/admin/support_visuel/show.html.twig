{% extends 'base.html.twig' %}

{% block title %}SupportVisuel{% endblock %}

{% block body %}
    <h1>SupportVisuel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ support_visuel.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ support_visuel.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ support_visuel.description }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ support_visuel.lien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('support_visuel_index') }}">back to list</a>

    <a href="{{ path('support_visuel_edit', {'id': support_visuel.id}) }}">edit</a>

    {{ include('support_visuel/_delete_form.html.twig') }}
{% endblock %}
